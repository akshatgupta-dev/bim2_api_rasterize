services:
  - type: web
    name: bim2-api-rasterize
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main_api:app --host 0.0.0.0 --port $PORT
